# Import all the models here so that Alembic/SQLAlchemy can detect them
# For our initial fallback skeleton, we declare the Base class here

from app.core.database import Base

# If we define database models (e.g. Job, Candidate) in the future,
# they would be imported here.
